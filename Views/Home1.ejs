<!-- Home1.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Result</title>
</head>
<body>
  <% if (number > 0) { %>
    <p>The number <%= number %> is positive.</p>
  <% } else if (number < 0) { %>
    <p>The number <%= number %> is negative.</p>
  <% } else { %>
    <p>The number <%= number %> is zero.</p>
  <% } %>
</body>
</html>
